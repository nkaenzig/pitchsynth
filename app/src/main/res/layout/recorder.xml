<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:background="@drawable/disk"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.work.synthesizer.Recorder" >

    <Button
        android:id="@+id/startRecord"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:text="Record" />

    <Button
        android:id="@+id/listenFromRecorder"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/startRecord"
        android:layout_marginTop="10dp"
        android:text="Listen" />
    
        <Button
        android:id="@+id/storeFromRecorder"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/listenFromRecorder"
        android:layout_marginTop="10dp"
        android:text="Store" />
        
        <Button
        android:id="@+id/playFromRecorder"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/storeFromRecorder"
        android:layout_marginTop="10dp"
        android:text="Play" />
        
        <Button
        android:id="@+id/recorderToMenu"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/playFromRecorder"
        android:layout_marginTop="10dp"
        android:text="Back" />

</RelativeLayout>